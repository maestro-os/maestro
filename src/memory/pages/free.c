#include <memory/pages/pages.h>

/*
 * Frees the given number of pages in the given memory region.
 * If `0` is specified for `pages`, then the whole block is freed.
 */
void pages_free(void *ptr, const size_t pages)
{
	// TODO
	(void) ptr;
	(void) pages;
}
